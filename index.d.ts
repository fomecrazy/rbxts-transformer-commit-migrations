export declare function $migrate(order: number): never;
export declare function $migrate(order: "__resolved", path: string[]): number;
export declare function $migrate(order: number | "__resolved", path: string[]): number | undefined;